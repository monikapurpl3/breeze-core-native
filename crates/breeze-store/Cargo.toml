[package]
name = "breeze-store"
version = "0.1.0"
description = "Byte-compatible reading and writing of Breeze Core's store files"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
