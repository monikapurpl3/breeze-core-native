{# Rendered by configd into /usr/local/etc/breeze-core/service.env - see
   +TARGETS. serve.sh reads it line by line and exports each NAME=value as
   data; it never sources it. The model has already refused malformed lines
   and the names the plugin sets itself. #}
{% if helpers.exists('OPNsense.BreezeCore.general.environment') %}{{ OPNsense.BreezeCore.general.environment }}
{% endif %}
